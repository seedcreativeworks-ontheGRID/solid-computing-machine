// Static-export stand-in for tasks.ts — see recommendations.static.ts for
// why this exists and why it can't just be a runtime env-var branch inside
// the real file.
/* eslint-disable @typescript-eslint/no-unused-vars */
export async function toggleTaskDone(taskId: string) {}
